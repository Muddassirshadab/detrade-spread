# detrade-spread
DeTrade Trade Executor Extension & Signal Server

This project contains:
- A Node.js Signal Server displaying a Web UI to send trades simultaneously.
- A Chrome Extension (Trade Executor) that connects to the server and executes the trades automatically.
